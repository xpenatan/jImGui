/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/

package gen.c.imgui;

import com.github.xpenatan.jParser.api.NativeObject;

public class ImDrawCallbackFunction extends NativeObject {

    static private ImDrawList ImDrawList_TEMP_STATIC_GEN_0;

    static private ImDrawCmd ImDrawCmd_TEMP_STATIC_GEN_0;

    private static final java.util.ArrayList<ImDrawCallbackFunction> TEAVMC_CALLBACKS = new java.util.ArrayList<>();

    private int teavmcCallbackId = -1;

    private int teavmcRegisterCallback() {
        if (teavmcCallbackId < 0) {
            teavmcCallbackId = TEAVMC_CALLBACKS.size();
            TEAVMC_CALLBACKS.add(this);
        } else {
            TEAVMC_CALLBACKS.set(teavmcCallbackId, this);
        }
        return teavmcCallbackId;
    }

    private static abstract class TEAVMC_onDraw_Function extends org.teavm.interop.Function {

        public abstract void call(int callbackId, long parent_list_addr, long draw_cmd_addr);
    }

    @org.teavm.interop.Export(name = "teavmc_ImDrawCallbackFunction_onDraw")
    private static void teavmc_ImDrawCallbackFunction_onDraw(int callbackId, long parent_list_addr, long draw_cmd_addr) {
        TEAVMC_CALLBACKS.get(callbackId).internal_onDraw(parent_list_addr, draw_cmd_addr);
    }

    @org.teavm.interop.Import(name = "teavmc_ImDrawCallbackFunction_setupCallback")
    private static native void setupCallback(long this_addr, int callbackId, TEAVMC_onDraw_Function onDraw_fp);

    static public final ImDrawCallbackFunction NULL = ImDrawCallbackFunction.native_new();

    /**
     * Dummy constructor, used internally to creates objects without C++ pointer
     */
    @Deprecated()
    protected ImDrawCallbackFunction(byte b, char c) {
    }

    /**
     * @return An empty instance without a native address
     */
    public static ImDrawCallbackFunction native_new() {
        return new ImDrawCallbackFunction((byte) 0, (char) 0);
    }

    protected void deleteNative() {
        internal_native_deleteNative(native_address);
    }

    @org.teavm.interop.Import(name = "imgui_imdrawcallbackfunction_deletenative")
    public static native void internal_native_deleteNative(long this_addr);

    public ImDrawCallbackFunction() {
        long addr = internal_native_create_addr();
        internal_reset(addr, true);
        setupCallback();
    }

    private void setupCallback() {
        int callbackId = teavmcRegisterCallback();
        setupCallback(native_address, callbackId, org.teavm.interop.Function.get(TEAVMC_onDraw_Function.class, ImDrawCallbackFunction.class, "teavmc_ImDrawCallbackFunction_onDraw"));
    }

    protected void onDraw(ImDrawList parent_list, ImDrawCmd draw_cmd) {
    }

    private void internal_onDraw(long parent_list_addr, long draw_cmd_addr) {
        if (ImDrawList_TEMP_STATIC_GEN_0 == null)
            ImDrawList_TEMP_STATIC_GEN_0 = ImDrawList.native_new();
        ImDrawList_TEMP_STATIC_GEN_0.internal_reset(parent_list_addr, false);
        if (ImDrawCmd_TEMP_STATIC_GEN_0 == null)
            ImDrawCmd_TEMP_STATIC_GEN_0 = ImDrawCmd.native_new();
        ImDrawCmd_TEMP_STATIC_GEN_0.internal_reset(draw_cmd_addr, false);
        onDraw(ImDrawList_TEMP_STATIC_GEN_0, ImDrawCmd_TEMP_STATIC_GEN_0);
    }

    @org.teavm.interop.Import(name = "imgui_imdrawcallbackfunction_create_addr")
    public static native long internal_native_create_addr();
}
