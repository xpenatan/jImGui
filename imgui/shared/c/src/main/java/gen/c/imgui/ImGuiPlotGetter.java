/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/

package gen.c.imgui;

import com.github.xpenatan.jParser.api.NativeObject;

public class ImGuiPlotGetter extends NativeObject {

    private static final java.util.ArrayList<ImGuiPlotGetter> TEAVMC_CALLBACKS = new java.util.ArrayList<>();

    private int teavmcCallbackId = -1;

    private int teavmcRegisterCallback() {
        if (teavmcCallbackId < 0) {
            teavmcCallbackId = TEAVMC_CALLBACKS.size();
            TEAVMC_CALLBACKS.add(this);
        } else {
            TEAVMC_CALLBACKS.set(teavmcCallbackId, this);
        }
        return teavmcCallbackId;
    }

    private static abstract class TEAVMC_onGetValue_Function extends org.teavm.interop.Function {

        public abstract float call(int callbackId, int index);
    }

    @org.teavm.interop.Export(name = "teavmc_ImGuiPlotGetter_onGetValue")
    private static float teavmc_ImGuiPlotGetter_onGetValue(int callbackId, int index) {
        return TEAVMC_CALLBACKS.get(callbackId).internal_onGetValue(index);
    }

    @org.teavm.interop.Import(name = "teavmc_ImGuiPlotGetter_setupCallback")
    private static native void setupCallback(long this_addr, int callbackId, TEAVMC_onGetValue_Function onGetValue_fp);

    static public final ImGuiPlotGetter NULL = ImGuiPlotGetter.native_new();

    /**
     * Dummy constructor, used internally to creates objects without C++ pointer
     */
    @Deprecated()
    protected ImGuiPlotGetter(byte b, char c) {
    }

    /**
     * @return An empty instance without a native address
     */
    public static ImGuiPlotGetter native_new() {
        return new ImGuiPlotGetter((byte) 0, (char) 0);
    }

    protected void deleteNative() {
        internal_native_deleteNative(native_address);
    }

    @org.teavm.interop.Import(name = "imgui_imguiplotgetter_deletenative")
    public static native void internal_native_deleteNative(long this_addr);

    public ImGuiPlotGetter() {
        long addr = internal_native_create_addr();
        internal_reset(addr, true);
        setupCallback();
    }

    private void setupCallback() {
        int callbackId = teavmcRegisterCallback();
        setupCallback(native_address, callbackId, org.teavm.interop.Function.get(TEAVMC_onGetValue_Function.class, ImGuiPlotGetter.class, "teavmc_ImGuiPlotGetter_onGetValue"));
    }

    protected float onGetValue(int index) {
        return 0;
    }

    private float internal_onGetValue(int index) {
        return onGetValue(index);
    }

    @org.teavm.interop.Import(name = "imgui_imguiplotgetter_create_addr")
    public static native long internal_native_create_addr();
}
